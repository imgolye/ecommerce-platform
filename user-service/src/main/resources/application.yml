server:
  port: 9001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/b2b2c_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

jwt:
  secret: b2b2c-platform-jwt-secret-key-must-be-at-least-32-characters-long-for-security
  expiration: 86400000
